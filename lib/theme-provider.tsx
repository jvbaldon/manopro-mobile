import React from 'react';

/**
 * ThemeProvider — MVP stub
 * Futuramente pode integrar dark mode com useColorScheme
 */
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
